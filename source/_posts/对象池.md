---
title: "对象池"
date: 2026-01-12 18:03:33
updated: 2026-01-12 18:03:33
categories:
  - ["编程语言与算法", "C/C++"]
tags:
  - "对象池"
  - "C++运算符重载"
description: "提前申请大内存，切割小内存使用，使用完回归大内存 与内存池原理相同"
cover: ""
---
``` c++
template <typename T>
class Queue
{
    public:
        Queue()
        {
            _front=_rear=new QueueItem();
        }
        ~Queue(){

        }
        void push(const T&val)
        {
            QueueItem item =new QueueItem(val);
            _rear->next=item; 
            _rear=item; 
        }
        void pop()
        {
            if(empty())
            {
                return;
            }
            QueueItem * //。。。
            //。。。。
        }
    private:
        struct QueueItem
        {
            QueueItem(T data=T()):_data(data),_next(nullptr){}; //传入默认参数data（类型为T，值为T的默认值）
            T _data;
            QueueItem * _next;
            
        }
        QueueItem *_front;
        QueueItem *_rear;

}
int main()
{
    Queue<int> que;
    for(int i=0;i<10000;i++>
    {
        que.push(i);    //反复new和delete，不划算-》对象池
        que.pop();
    })
}
```
# 池
## 内存池
提前申请大内存，切割小内存使用，使用完回归大内存
## 对象池
与内存池原理相同

优化在QueueItem（对象池）
``` c++
struct QueueItem    //产生100000个对象的对象池
{
    QueueItem(T data=T()):_data(data),_next(nullptr){}; //传入默认参数data（类型为T，值为T的默认值）
    void *operator new(size_t size) //size_t是一种类型，很大（比int double之类都大），任何类型指针都可以返回为void*
    {

    }
    T _data;
    QueueItem * _next;
            
}

```


### new与delete的重载
``` c++
class a
{
public:
	void* operator new(size_t size)
	{
        return malloc(size);
	}
    void operator delete(void* ptr)
	{
        free(ptr);
	}
};

```
# 对象池设计
![20260112213720](https://cdn.jsdelivr.net/gh/Yolo-ZZY/Image/20260112213720.png)
## 策略接口
``` c++
template <typename T>
class Allocator
{
public：
    virtual T *allocate()=0；
    virtual void deallocate(T *p)=0；
}



```
